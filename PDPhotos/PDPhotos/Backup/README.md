# Backup folder

ATM contains protocols related to photos backup. Implementation is left in `ProtonDrive-iOS` for now, since it's wired with old Photos UI.

Once old Photos UI is removed, we can move all the backup implementation here.
